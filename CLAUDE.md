# 開発

以下を参照して AI の担当領域を把握すること。

```sh
# ~/.claude/CLAUDE.md は hatano299 リポジトリへの symlink
repo_root="$(dirname "$(readlink -f ~/.claude/CLAUDE.md)")"
```

- `$repo_root/docs/dev-strategy.md` — 開発戦略
- `$repo_root/docs/dev-reference.md` — 開発手引き
